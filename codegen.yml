schema: 'http://localhost:3000/api/gql'

generates:
  types/client-gql-generated.d.ts:
    documents:
      - 'src/**/!(*.d).{ts,tsx}'
    config:
      noExport: true
    plugins:
      - typescript
      - typescript-operations